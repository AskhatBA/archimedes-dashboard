services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
    image: archimedes-dashboard:latest
    ports:
      - "8080:80"
    restart: unless-stopped
